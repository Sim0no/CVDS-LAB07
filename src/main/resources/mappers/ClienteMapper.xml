<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="edu.eci.cvds.sampleprj.dao.mybatis.mappers.ClienteMapper">

	<select parameterType="map" id="consultarClientes" resultMap="ClienteResult">
                    select c.nombre,c.documento,c.telefono,c.direccion,c.email,c.vetado,ir.id as ItemRentado_id ,ir.fechainiciorenta as ItemRentado_fechainiciorenta ,ir.fechafinrenta as ItemRentado_fechafinrenta,i.id as Item_id ,i.nombre as Item_nombre ,i.descripcion as Item_descripcion , i.fechalanzamiento as Item_fechalanzamiento ,i.tarifaxdia as Item_tarifaxdia,i.formatorenta as Item_formatorenta,i.genero as Item_genero , ti.id as,ti.descripcion 

		    
		    FROM VI_CLIENTES as c 
		    left join VI_ITEMRENTADO as ir on c.documento=ir.CLIENTES_documento 
		    left join VI_ITEMS as i on ir.ITEMS_id=i.id 
		    left join VI_TIPOITEM as ti on i.TIPOITEM_id=ti.id;
	</select>
	<resultMap type='Cliente' id='ClienteResult'>
		<id property='documento' column='documento'/>
		<result property='nombre' column='nombre'/>
		<result property='telefono' column='telefono'/>
		<result property='direccion' column='direccion'/>
		<result property='email' column='email'/>
		<result property='vetado' column='vetado'/>
		<collection property='rentados' javaType='ArrayList' ofType='ItemRentado' resultMap='edu.eci.cvds.sampleprj.dao.mybatis.mappers.ItemRentadoMapper' columnPrefix='ItemRentado_'>
		</collection>
	</resultMap>
</mapper>